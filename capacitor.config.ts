import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mysoulmate.mysoulmate_App',
  appName: 'My Soulmate',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
