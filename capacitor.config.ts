import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'mysoulmate.co.in',
  appName: 'My Soulmate',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
